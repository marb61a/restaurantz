<header class="head-section">
  <div class="navbar navbar-default navbar-static-top container">
      <div class="navbar-header">
          <button class="navbar-toggle" data-target=".navbar-collapse" data-toggle="collapse" type="button">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to root_path, class: "navbar-brand" do %>
            R<span>estaurantz</span>
          <% end %>
      </div>
      <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
              <li class="dropdown">
                <%= link_to pages_path, data: { close_others: false, delay: 0, hover: "dropdown"} %>
                  <i class="fa fa-home"></i> Home
                <% end %>
              </li>
              <li class="dropdown">
                <%= link_to root_path, data: {close_others: false, delay: 0, hover: "dropdown"} %>
                  <i class="fa fa-cutlery"></i> Restaurants 
                <% end %>
              </li>
              <li class="dropdown">
                <%= link_to pages_about_path, data: {close_others: false, delay: 0, hover: "dropdown"} %>
                  <i class="fa fa-info-circle"></i> About Us
                <% end %>    
              </li>
              <li>
                <%= link_to pages_contact_path, data: {close_others: false, delay: 0, hover: "dropdown"} %>
                  <i class="fa fa-envelope"></i> Contact Us
                <% end %>
              </li>
          </ul>
      </div>
  </div>
</header>